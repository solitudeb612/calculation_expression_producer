server:
  port: 9999

spring:
  datasource:
    username: root
    password: 88888888@yyh
    url: jdbc:mysql://localhost:3306/calculation
    driver-class-name: com.mysql.cj.jdbc.Driver

redis:
  port: 6379
  host: localhost


logging:
  level:
    com.yyh: debug

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
